import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  server: {
    proxy: {
      // Dès que React appelle une URL qui commence par '/api',
      // le serveur de Vite va intercepter la requête et la rediriger lui-même vers Laravel
      '/api': {
        target: 'http://127.0.0.1:8000',
        changeOrigin: true,
        secure: false,
      }
    }
  }
})